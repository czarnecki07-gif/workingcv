{
  "name": "workingcv-fixed",
  "version": "2.1.0",
  "private": true,
  "description": "Vercel-ready static WorkingCV app with landing, templates, local save and PDF export",
  "scripts": {
    "start": "npx serve public",
    "dev": "npx serve public"
  }
}
